// Use OpenAI's API for the magic: personalized learning and stuff

/**
 * (placeholder)
 *
 * Generate a title for a resource given some text (e.g. a URL or snippet of contents)
 */
export function createTitle(text: string): string {
	return text;
}
